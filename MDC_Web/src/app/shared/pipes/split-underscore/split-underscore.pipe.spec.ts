import { SplitUnderscorePipe } from './split-underscore.pipe';

describe('SplitUnderscorePipe', () => {
  it('create an instance', () => {
    const pipe = new SplitUnderscorePipe();
    expect(pipe).toBeTruthy();
  });
});
