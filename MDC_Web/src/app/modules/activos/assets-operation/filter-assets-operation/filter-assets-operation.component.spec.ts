/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { FilterAssetsOperationComponent } from './filter-assets-operation.component';

describe('FilterAssetsOperationComponent', () => {
  let component: FilterAssetsOperationComponent;
  let fixture: ComponentFixture<FilterAssetsOperationComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ FilterAssetsOperationComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(FilterAssetsOperationComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
