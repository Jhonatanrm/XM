/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TableAssetsOperationComponent } from './table-assets-operation.component';

describe('TableAssetsOperationComponent', () => {
  let component: TableAssetsOperationComponent;
  let fixture: ComponentFixture<TableAssetsOperationComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TableAssetsOperationComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TableAssetsOperationComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
