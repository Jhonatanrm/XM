/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SubestacionConfiguracionComponent } from './subestacion-configuracion.component';

describe('SubestacionConfiguracionComponent', () => {
  let component: SubestacionConfiguracionComponent;
  let fixture: ComponentFixture<SubestacionConfiguracionComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SubestacionConfiguracionComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SubestacionConfiguracionComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/