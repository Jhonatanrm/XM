/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { BahiaCorteCentralCrearComponent } from './bahia-corte-central-crear.component';

describe('BahiaCorteCentralCrearComponent', () => {
  let component: BahiaCorteCentralCrearComponent;
  let fixture: ComponentFixture<BahiaCorteCentralCrearComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ BahiaCorteCentralCrearComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(BahiaCorteCentralCrearComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/