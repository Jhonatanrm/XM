/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PlantatermicaComponent } from './plantatermica.component';

describe('PlantatermicaComponent', () => {
  let component: PlantatermicaComponent;
  let fixture: ComponentFixture<PlantatermicaComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PlantatermicaComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PlantatermicaComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/