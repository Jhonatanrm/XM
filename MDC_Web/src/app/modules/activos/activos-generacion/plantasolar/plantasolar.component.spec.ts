/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PlantasolarComponent } from './plantasolar.component';

describe('PlantasolarComponent', () => {
  let component: PlantasolarComponent;
  let fixture: ComponentFixture<PlantasolarComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PlantasolarComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PlantasolarComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/