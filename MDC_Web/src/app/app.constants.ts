import { environment } from '@env/environment';

export const globalConstants = {
    redirectUrls: {
        url_1_1: `/transmision/createProject/`, // reemplazar por url proyecto transmision

        url_1_2: `/activos-transmision/`, // reemplazar por url Aprobar parámetros técnicos

        url_1_3: `/transmision/singleLineDiagram/`, // reemplazar por url diagrama unifilar

        url_2_4: `/requirements/`, // reemplazar por url solicitd de cambio Subestacion

        url_2_5: `/requirements/` // reemplazar por url solicitd de cambio Barra
    }
};
